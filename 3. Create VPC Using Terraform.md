Create a VPC named `xfusion-vpc` in region `us-east-1` with any `IPv4` CIDR block through terraform.

The Terraform working directory is `/home/bob/terraform`. 
Create the `main.tf` file (do not create a different `.tf` file) to accomplish this task.


## 🚀 Solution Steps

1. In the `aws-client` navigate to `/home/bob/terraform` directory
2. create a new `main.tf` file

```
vi main.tf
``` 

3. Into the directory write the next terraform code lines.


```
resource "aws_vpc" "xfusion-vpc"{
    cidr_block = "10.0.0.0/20"

    tags = {
        Name = "xfusion-vpc"
    }
}
```

**Note:** 
Despide, the task doesn´t requiered to write tags, it´s important to create a task in order to achieve the task.

## 🔍 Links

-Resource: VPC 
https://registry.terraform.io/providers/-/aws/6.8.0/docs/resources/vpc
